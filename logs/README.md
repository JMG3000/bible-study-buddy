# Logs

This directory separates commit-safe summaries from raw local evidence.

- `logs/local/` is ignored by git and stores raw local validation logs.
- `docs/audits/` stores sanitized summaries that are safe to commit.
